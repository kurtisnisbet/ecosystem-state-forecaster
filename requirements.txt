# Runtime dependencies for the hosted demo (app/streamlit_app.py).
#
# Deliberately slim. The app reads precomputed artifacts from docs/app_data and
# imports no model or geospatial libraries, so the hosted container stays small
# and starts fast. Streamlit Community Cloud installs from this file.
#
# For the full research pipeline (DEA access, LightGBM, PyTorch), use:
#   pip install -r requirements-dev.txt

streamlit>=1.41
numpy==2.4.6
pandas==3.0.3
xarray==2026.4.0
netcdf4==1.7.2
matplotlib==3.11.0
