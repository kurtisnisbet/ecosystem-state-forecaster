# Core dependencies for the Ecosystem State Forecaster (v1, data + EDA phase).
# Pinned for reproducibility. Modelling libs (scikit-learn, xgboost, torch)
# will be added when we reach those stages.

# ── Cube core ─────────────────────────────────────────────
numpy==2.4.6
pandas==3.0.3
xarray==2026.4.0
dask==2026.6.0

# ── Geospatial I/O ────────────────────────────────────────
rioxarray==0.19.0
rasterio==1.4.4
geopandas==1.1.4
shapely==2.1.2

# ── DEA / STAC access ─────────────────────────────────────
pystac-client==0.9.0
odc-stac==0.5.2

# ── Exploration & viz ─────────────────────────────────────
matplotlib==3.11.0
jupyterlab==4.6.0
ipykernel==7.3.0
