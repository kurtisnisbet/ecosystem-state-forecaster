"""Honest spatio-temporal evaluation.

Responsibilities:
- Walk-forward temporal folds with an embargo gap.
- Spatial blocks with buffers (sizes derived from a variogram range).
- Report skill vs persistence/climatology, uncertainty, and error maps,
  across the space x time 2x2.
"""

# TODO: walk_forward_splits(), spatial_blocks(), skill_score(), error_maps()
